<Window x:Class="whisperMeOff.VuMeterWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="VU Meter"
        Width="300" Height="60"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize">
    <Border Background="#E0000000" CornerRadius="8" Padding="10,5">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="5"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Text="Recording..." Foreground="White" FontSize="12" HorizontalAlignment="Center" FontWeight="SemiBold"/>
            <ProgressBar x:Name="VuMeter" Grid.Row="2" Height="20" Minimum="0" Maximum="100" Value="0" 
                        Background="#404040" Foreground="#00FF00" BorderThickness="0"/>
        </Grid>
    </Border>
</Window>
